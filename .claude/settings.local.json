{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q 'git commit'; then codex review --uncommitted 'Check for bugs/security issues only. Be brief.' 2>/dev/null || true; fi"
          }
        ]
      }
    ]
  }
}
